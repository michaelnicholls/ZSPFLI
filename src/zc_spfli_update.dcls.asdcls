@EndUserText.label: 'access control'
@MappingRole: true
define role ZC_SPFLI_UPDATE {
    grant 
        select
            on
  ZC_SPFLI_update
                    where
                        (Carrid) = aspect pfcg_auth(S_CARRID, CARRID , ACTVT = '02');}